# Hudson Orchestration Log — Viewer Test Infrastructure (IN PROGRESS)

**Timestamp:** 2026-06-24T12:41:40-05:00  
**Agent:** Hudson (Tester, sonnet)  
**Phase:** Phase 1 — Real Fault Viewer  
**Status:** IN PROGRESS — adding tests for new readers + coordinate mapping regression guard

---

## Assignment

Add integration tests for the real fault viewer implementation:
1. Round-trip test: bake script → zarr read → verify output shape/range
2. Coordinate mapping regression test: fault stick parsing → correct abs coords
3. Reader function tests: `_get_amplitude_slice()`, `_get_fault_prob_slice()`

## Current Status

Identified scope:
- Test bake script produces valid zarr v3 files with correct dtypes/shapes
- Test viewer coordinate transformation (0-based internal → absolute coords)
- Test fault stick parsing (`_load_fault_sticks()`) maps z_ms sample indices correctly
- Guard against regression on inline slider bounds (1001–1100, not 1000–1200)

## Deferred Tests (Phase 2)

- Streamlit widget state (threshold slider, inline selection)
- Full UI rendering test (requires headless browser)
- Wiggle trace overlay validation (deferred feature)

## Expected Deliverable

**File:** `.squad/decisions/inbox/hudson-viewer-test-findings.md` (to be merged next session)

Will document:
- Test coverage summary (coordinator utils, zarr readers, coordinate transforms)
- Any discovered edge cases (e.g., boundary slices, empty fault slices)
- Recommendations for pre-demo validation

## Outcome

📝 IN PROGRESS — to be completed and merged at next orchestration cycle.
