# Orchestration Log — Dallas (Data/ML Engineer)

**Session:** Releasing #37 fix, UI redesign  
**Date:** 2026-07-14T13:45:29Z  
**Coordinator:** Ripley + Squad

---

## Summary

Two major contributions this session:

1. **v0.7.3 Best-Checkpoint Loss-Fallback Fix** — Fixed a bug in `_select_best_checkpoint` where the `not best_saved` guard permanently blocked after epoch 1, causing `best.pt` to always capture the epoch-1 checkpoint even when later epochs had lower loss. Changed guard to `val_metrics["iou"] >= best_val_iou and val_metrics["loss"] < best_val_loss`. Added 2 regression tests. Commit: 1d184c6. Hudson independently verified (regression test fails on old code, passes on new). All 391 tests pass, ruff clean. Released as v0.7.3.

2. **F3 Real-Data Ingest Fixes (PR #32)** — Delivered irregular SEG-Y fallback + F3 native fault-export format support. Broadened error trigger for irregular geometry detection, added `_extract_corner_tie_points` for world-coordinate georeference in sidecar, and implemented `parse_f3_fault_sticks` for headerless 5-column map-coordinate F3 fault exports. 12 regression tests. Geospatially verified: all 18 F3 fault points fall inside volume bounds.

---

## Artifacts

- **Commit 1d184c6:** `chore(training): best-checkpoint loss-fallback fix (#37 regression test guard)`
- **PR #32 (merged):** F3 irregular SEG-Y + fault-parser support; `.squad/skills/dockerfile-dep-pinning/SKILL.md` tracking
- **Tests:** `src/tests/test_sprint2_training.py` (+2 regression), `src/tests/test_ingest/test_f3_realdata.py` (+12)

---

## Impact

- v0.7.3 shipped and CD green; infra re-run requested on warm T4 (deepseismic2-infra#19)
- F3 ingest blocker removed; real F3 ingestion + training now viable
- Pipeline confidence improved with loss-fallback guarantee + real geographic validation

---

## Next: Infra T4 Re-run

Issue #37 remains OPEN pending re-run results (non-zero val IoU@best-threshold, real AP, best.pt with non-epoch-1 metrics).
