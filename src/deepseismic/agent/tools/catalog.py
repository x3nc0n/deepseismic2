"""Dataset catalog tool definitions for the analyst agent.

This module will expose tool contracts that let the agent search dataset metadata,
resolve survey context, and answer discovery-oriented questions over indexed
catalog information. Planned interfaces include dataset search inputs, result
ranking metadata, and grounding-friendly response envelopes.
"""
