"""Run status tool definitions for the analyst agent.

This module will expose tool contracts that let the agent inspect ingest,
preprocessing, and inference job state. Planned interfaces include run lookup by
identifier, status normalization across execution backends, and concise summaries
for analyst-facing responses.
"""
